#include <stdio.h>
#include <stdlib.h>
/**
 * main - main block
 * Description: prints alphabet in lowercase, and in uppercase
 * followed by new line
 * Return: 0
 */
int main(void)
{
	char c = 'a' 'A';

	while (c <= 'z' 'Z')
	{
		putchar(C);
		c++;
	}

	putchar('\n');
	return (0);
}
