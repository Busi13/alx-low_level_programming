#include "main.h"

/**
 * more_numbers - prints 10 times the numbers, from 0 to 14,
 * followed by a new line
 * Return: 0-14 10times
 */

void more_numbers(void)
{
	int i, j;

	for (ri = 1; i <= 10; i++)
	{
		for (j = 0; j <= 14; i++)
		{
			if (j >= 10
			_putchar('i')
		_putchar(j % 10 + '0');
	}
	_putchar('\n');
}
